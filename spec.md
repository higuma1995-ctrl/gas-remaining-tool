# spec.md
# gas-remaining-tool
# version: 1.0
# 作成日: 2026-04-15
# 注意: 実装AIが唯一従う仕様。変更はCHANGELOG→spec→requirementsの順で行う。

---

## ⚠️ 実装禁止事項（最優先）
- 外部API使用禁止
- specに記載のない機能追加禁止
- 既存の動作するコードを理由なく変更禁止
- フェーズ2項目（複数本セット・47L容器等）の先行実装禁止

---

## 1. 画面構成

単一画面（SPA）。縦1カラム・入力→結果の一直線レイアウト。モーダルなし。

```
┌─────────────────────────┐
│ ヘッダー（アプリ名）        │
├─────────────────────────┤
│ 単位切替トグル（MPa / kgf/cm²）│
├─────────────────────────┤
│ 容器サイズ入力             │
│（ボタン選択 + 手入力）      │
├─────────────────────────┤
│ 充填圧力入力               │
│（ボタン選択：14.7 / 19.6）  │
├─────────────────────────┤
│ 現在圧力入力               │
│（スライダー + 数値表示）     │
├─────────────────────────┤
│ 流量入力                  │
│（ボタン選択 + 手入力）      │
├─────────────────────────┤
│ 結果表示エリア              │
│（残量L・残り時間・警告色）   │
├─────────────────────────┤
│ 注意書き                  │
└─────────────────────────┘
```

---

## 2. データ構造

### 入力値（localStorageで保存）
```json
{
  "unit": "MPa" | "kgf/cm²",
  "volume": number,
  "fillPressure": 14.7 | 19.6,
  "currentPressure": number,
  "flowRate": number
}
```

### localStorage キー
| キー | 内容 |
|------|------|
| `grt_inputs` | 入力値オブジェクト（JSON） |

---

## 3. 各入力項目の仕様

### 3-1. 単位切替トグル
- 選択肢：MPa / kgf/cm²
- 切り替え時：現在圧力スライダーの範囲・表示を即時更新
- デフォルト：MPa

### 3-2. 容器サイズ（内容積）
- プリセットボタン：1.1L / 2.0L / 2.8L / 3.4L / 10L
- 手入力フィールド：数値入力（正の数のみ）
- プリセット選択時は手入力フィールドに値を反映
- 手入力時はプリセットボタンの選択状態を解除
- 単位表示：L

### 3-3. 充填圧力
- プリセットボタン：14.7MPa / 19.6MPa の2択
- kgf/cm²表示時：14.7MPa → 150 kgf/cm²、19.6MPa → 200 kgf/cm²
- 手入力なし（2択固定）
- デフォルト：14.7MPa

### 3-4. 現在圧力（スライダー）
- 範囲：0 〜 充填圧力の値
- ステップ：MPa時 0.1 / kgf/cm²時 1
- スライダーの横に現在値をリアルタイム表示
- 単位切替時：値を換算して引き継ぐ（MPa×10 = kgf/cm²）

### 3-5. 流量（L/分）
- プリセットボタン：0.25 / 0.5 / 1.0 / 1.5 / 2.0 / 3.0 / 4.0 / 5.0
- 手入力フィールド：数値入力（正の数のみ）
- プリセット選択時は手入力フィールドに値を反映
- 手入力時はプリセットボタンの選択状態を解除
- 単位表示：L/分

---

## 4. 計算仕様

### 4-1. 計算式（MPa表記）
```
残量(L) = 内容積(L) × 現在圧力(MPa) × 10
使用可能時間(分) = 残量(L) ÷ 流量(L/分)
```

### 4-2. 計算式（kgf/cm²表記）
```
残量(L) = 内容積(L) × 現在圧力(kgf/cm²)
使用可能時間(分) = 残量(L) ÷ 流量(L/分)
```

### 4-3. 計算タイミング
- 入力値が変わるたびにリアルタイム計算・表示更新

### 4-4. 表示形式
- 残量：小数点第1位まで（例：34.0L）
- 使用可能時間：分単位・整数（例：68分）、60分以上は「X時間Y分」表記

---

## 5. 結果表示・警告仕様

### 5-1. 警告色の基準
充填圧力に対する現在圧力の割合で判定：

| 割合 | 表示色 | 意味 |
|------|--------|------|
| 50%超 | 緑（normal） | 十分な残量 |
| 30%〜50% | 黄（warning） | 交換を検討 |
| 30%未満 | 赤（danger） | 早急に交換 |

- 結果エリア全体の背景色・テキスト色で視覚的に表現
- 色覚対応：色だけでなくアイコンまたはテキストラベルも併用

### 5-2. 注意書き
結果エリアの下に固定表示：
```
※実際の残量には誤差があります。余裕を持って交換してください。
```

### 5-3. 未入力・エラー時
- 必須項目（容器サイズ・現在圧力・流量）が未入力の場合：結果エリアに「入力値を確認してください」を表示
- 流量が0の場合：「流量を入力してください」を表示
- 負の値・異常値：入力を受け付けない（HTMLのmin属性で制御）

---

## 6. 永続化仕様

- 全入力値をlocalStorageに保存（`grt_inputs`）
- 保存タイミング：入力値変更のたびに即時保存
- 起動時：保存済みの値があれば復元して表示
- 保存失敗：サイレント失敗（エラー表示なし）

---

## 7. 非スコープ（実装しないもの）

- 複数本ボンベの合算計算
- 集合装置対応
- 47L以上の大型容器プリセット
- アラーム・通知機能
- ログ・履歴機能
- 外部API
- ログイン・同期

---

## 8. 技術仕様

- フレームワーク：Vite + React
- スタイル：CSS Modules または Tailwind（実装者判断）
- データ保存：localStorageのみ
- 外部ライブラリ：使用しない（UIコンポーネントライブラリも不要）
- ホスティング：GitHub Pages
- CI/CD：GitHub Actions（deploy-pages.yml）
- ビルド出力：`dist/`
- base URL：`vite.config.js` で `/リポジトリ名/` を設定

---

## CHANGELOG
| 日付 | バージョン | 変更内容 |
|------|-----------|---------|
| 2026-04-15 | 1.0 | 初版作成 |
