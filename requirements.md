# requirements.md
_最終更新：2026-04-14_

---

## 1. 目的

高圧ガス容器（酸素ボンベ等）の残量・使用可能時間の計算は、検索と電卓を組み合わせた手間のかかる作業になりがちで、かつ素人が行う場面も多い。本ツールはその計算を即座に・正確に・わかりやすく提供し、現場での安全確保を支援する。

判断の最終責任は使用者が持ち、本ツールはあくまで簡易計算の補助として提供する。

---

## 2. 対象ユーザー

| ユーザー層 | 使い方 |
|-----------|--------|
| 在宅酸素患者・家族 | 外出前・就寝前に残量と使用可能時間を確認 |
| 介護職 | 訪問先でボンベ残量を素早く確認 |
| 医療職 | 搬送前の残量・使用可能時間の計算 |

---

## 3. 対象スコープ（フェーズ1）

小型ボンベ（在宅・介護・搬送シーン）に絞る。大型ボンベ・集合装置はフェーズ2以降。

---

## 4. 計算仕様

### 4-1. 計算式（MPa表記）
```
残量(L) = 内容積(L) × 現在圧力(MPa) × 10
使用可能時間(分) = 残量(L) × 0.8（安全係数） ÷ 流量(L/分)
```

### 4-2. 計算式（kgf/cm²表記）
```
残量(L) = 内容積(L) × 現在圧力(kgf/cm²)
使用可能時間(分) = 残量(L) × 0.8（安全係数） ÷ 流量(L/分)
```

### 4-3. 入力項目
| 項目 | 内容 |
|------|------|
| 内容積 | プリセット選択（1.1L / 2.0L / 2.8L / 3.4L / 10L）または手動入力 |
| 充填圧力 | プリセット選択（14.7MPa / 19.6MPa）または手動入力 |
| 現在圧力 | 数値入力（圧力計の指示値） |
| 流量 | 数値入力（L/分） |
| 圧力単位 | MPa（デフォルト） / kgf/cm²の切り替え |

---

## 5. スコープ

### 5-1. 計算画面
- 入力項目をすべて入力すると残量・使用可能時間をリアルタイム表示
- アナログ圧力計風ゲージを表示（0〜20MPa / 0〜200kgf/cm²）
  - 針が現在圧力を指す
  - 数値入力と連動（入力すると針が動く）
  - 残量割合に応じて色が変わる（緑→黄→赤）
- 結果表示：残量(L)・使用可能時間（分・時間）を表示
- 圧力単位の注意書き：「現在の圧力計の単位を確認してください。一般的にはMPaが多いです」

### 5-2. 満タンボタン
- 前回保存された現在圧力が充填圧力未満の場合のみ表示
- タップで現在圧力を充填圧力と同じ値にセット

### 5-3. メモリ
- 前回の入力値を自動保存・次回起動時に自動復元
- よく使う設定をお気に入りとして手動保存（名前をつけて管理）
- お気に入りからワンタップで計算画面にセット

### 5-4. アラーム
- 残り使用可能時間の割合でアラームを設定
- デフォルト：75% / 50% / 25%の3段階
- ユーザーが追加・変更・削除可能
- 表示形式：「50%（約3時間30分）」のように割合と時間を併記
- PWA通知で実装

### 5-5. 履歴
- 計算結果を日時・設定値・結果とともに自動保存
- 一覧表示・タップで設定を復元
- 個別削除・全消去可能

### 5-6. 設定画面
- お気に入り管理（追加・削除・並び替え）
- アラーム設定（割合・追加・削除）
- 圧力単位の切り替え（MPa / kgf/cm²）

### 5-7. 画面構成
- ボトムナビゲーション（タブバー）構成
- タブ①「計算」：メイン計算・圧力計ゲージ・結果表示・満タンボタン
- タブ②「履歴」：計算履歴一覧・設定復元
- タブ③「設定」：お気に入り・アラーム・単位切替

---

## 6. やらないこと（フェーズ1）

- 大型ボンベ（47L等）対応
- 集合装置（複数本セット）計算
- 外部API使用
- ログイン・同期機能
- 医療的アドバイス・診断

---

## 7. 免責事項（画面に表示）

「本ツールは簡易計算の補助を目的としています。実際の使用にあたっては必ず圧力計を直接確認し、判断は使用者自身の責任で行ってください。」

---

## 8. 技術構成

| 項目 | 内容 |
|------|------|
| フレームワーク | Vite + React |
| データ保存 | localStorageのみ |
| 通知 | PWA通知API |
| ホスティング | GitHub Pages |
| 外部API | 使用しない |

---

## 9. 成功条件

- 入力後即座に残量・使用可能時間が表示されること
- 圧力計ゲージが直感的でわかりやすいこと
- 前回の値が自動復元されること
- アラームが指定タイミングで通知されること
- spec.mdとコードが完全一致していること
- GitHub Pagesで公開済みであること
